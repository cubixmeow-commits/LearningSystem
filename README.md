# Clean slate

This repository has been emptied so a new project can be built from what we learned.

`.cpanel.yml` is kept so cPanel Git Version Control can still deploy to
`/home/iainmcok/public_html/learn/`.
